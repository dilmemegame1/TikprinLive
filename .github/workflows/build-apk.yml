name: Crear APK TikPrint LIVE

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar proyecto
        uses: actions/checkout@v4

      - name: Instalar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Instalar Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Descomprimir proyecto
        run: |
          unzip -o TikPrintLIVE_Android_Prototype.zip
          cd TikPrintLIVE
          gradle assembleDebug

      - name: Guardar APK
        uses: actions/upload-artifact@v4
        with:
          name: TikPrintLIVE-APK
          path: TikPrintLIVE/app/build/outputs/apk/debug/app-debug.apk
